import React from 'react';
import {IconButton} from 'react-mdl';

export default class SongMenuButton extends React.PureComponent {
    render() {
        return <IconButton name="play_circle_outline" />;
    }
}